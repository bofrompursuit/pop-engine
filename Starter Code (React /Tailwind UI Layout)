export default function PopEngineIntake() {
  return (
    <div className="min-h-screen bg-slate-50 text-slate-900 antialiased font-sans">
      {/* Top Header Bar */}
      <header className="sticky top-0 z-40 border-b border-slate-200 bg-white/80 backdrop-blur-md">
        <div className="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <span className="font-mono text-xs px-2 py-1 bg-slate-900 text-white rounded uppercase tracking-widest">
              POPENGINE
            </span>
            <span className="text-xs font-mono text-slate-400">|</span>
            <h1 className="text-sm font-semibold text-slate-900">NYC Event Permitting Intake</h1>
          </div>
          <div className="text-xs font-mono text-slate-500 hidden sm:block">
            LAT/LONG: <span className="text-slate-900 font-medium">40.7128° N, 74.0060° W</span>
          </div>
        </div>
      </header>

      {/* Main Form Body */}
      <main className="max-w-4xl mx-auto px-6 py-10 space-y-8">
        <section className="space-y-2 border-b border-slate-200 pb-6">
          <div className="text-xs font-mono uppercase tracking-widest text-slate-400">Step 01 / 03</div>
          <h2 className="text-3xl font-bold tracking-tight text-slate-900">Describe your event</h2>
          <p className="text-sm text-slate-600 max-w-2xl">
            Answer what applies to your event. Questions appear as your answers make them relevant, and “I don’t know” is stored as unknown and carried into your plan.
          </p>
        </section>

        {/* Dynamic Card Container */}
        <section className="space-y-6">
          <div className="bg-white rounded-xl border border-slate-200 p-6 shadow-sm space-y-4">
            <div className="flex items-center justify-between border-b border-slate-100 pb-3">
              <label className="text-sm font-semibold text-slate-900 uppercase tracking-wider">
                Structure types
              </label>
              <span className="text-xs font-mono text-amber-800 bg-amber-50 border border-amber-200 px-2 py-0.5 rounded">
                PERMIT TRIGGER: DOB
              </span>
            </div>
            
            <div className="grid grid-cols-2 sm:grid-cols-4 gap-3">
              {['Tent canopy', 'Stage platform', 'Prop truss', 'Bleachers inflatable'].map((item) => (
                <button
                  key={item}
                  type="button"
                  className="p-3 text-xs font-medium border border-slate-200 rounded-lg text-slate-700 hover:border-slate-900 hover:bg-slate-50 transition-all text-left"
                >
                  {item}
                </button>
              ))}
            </div>
          </div>
        </section>

        {/* Action Footer */}
        <footer className="pt-6 border-t border-slate-200 flex items-center justify-between">
          <button className="text-xs font-mono text-slate-500 hover:text-slate-900">
            Reset Intake
          </button>
          <button className="px-6 py-3 bg-slate-900 text-white font-medium text-sm rounded-lg shadow hover:bg-slate-800 transition-colors">
            Save event & view compliance plan
          </button>
        </footer>
      </main>
    </div>
  );
}
